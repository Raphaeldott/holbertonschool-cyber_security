All about fast incident response


Task 1:

Trouver quelle adresse IP apparaît le plus souvent dans un fichier de logs (logs.txt).
Dans un DoS, une IP (ou un groupe d’IP) envoie énormément de requêtes.
Donc l’IP la plus fréquente est probablement l’attaquant.
